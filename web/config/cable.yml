development:
  adapter: async

test:
  adapter: test

production:
  adapter: async
  # Switch to redis for production:
  # adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
